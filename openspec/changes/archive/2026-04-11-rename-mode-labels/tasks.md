## 1. 標籤重新命名

- [x] 1.1 在 `Sources/GoodTimer/ContentView.swift` 將 mode toggle 的 `"COUNTDOWN"` label 改為 `"TIMER"`，`"COUNT UP"` 改為 `"STOPWATCH"`（Compact mode hides button labels below 400pt width）

## 2. 驗證

- [x] 2.1 編譯確認無錯誤，啟動 app 確認模式切換按鈕顯示 TIMER/STOPWATCH
